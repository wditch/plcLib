void customIO() {
  // Input pin directions
  pinMode(I00, INPUT);
  pinMode(I01, INPUT);
  pinMode(I02, INPUT);
  pinMode(I03, INPUT);
  pinMode(I04, INPUT);
  pinMode(I05, INPUT);
  pinMode(I06, INPUT);
  pinMode(I07, INPUT);
  pinMode(I08, INPUT);
  pinMode(I09, INPUT);
  
  // Output pin directions
  pinMode(Q00, OUTPUT);
  pinMode(Q01, OUTPUT);
  pinMode(Q02, OUTPUT);
  pinMode(Q03, OUTPUT);
  pinMode(Q04, OUTPUT);
  pinMode(Q05, OUTPUT);
  pinMode(Q06, OUTPUT);
  pinMode(Q07, OUTPUT);
  pinMode(Q08, OUTPUT);
  pinMode(Q09, OUTPUT);
}

