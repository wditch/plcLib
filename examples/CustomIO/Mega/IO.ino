void customIO() {
  // Input pin directions
  pinMode(A0, INPUT);
  pinMode(A1, INPUT);
  pinMode(A2, INPUT);
  pinMode(A3, INPUT);
  pinMode(A4, INPUT);
  pinMode(A5, INPUT);
  pinMode(A6, INPUT);
  pinMode(A7, INPUT);
  pinMode(A8, INPUT);
  pinMode(A9, INPUT);
  pinMode(A10, INPUT);
  pinMode(A11, INPUT);
  pinMode(A12, INPUT);
  pinMode(A13, INPUT);
  pinMode(A14, INPUT);
  pinMode(A15, INPUT);
  
  // Input/Output pin directions (delete / enable as appropriate)
  pinMode(D0, OUTPUT);
  // pinMode(D0, INPUT);
  pinMode(D1, OUTPUT);
  // pinMode(D1, INPUT);
  pinMode(D2, OUTPUT);
  // pinMode(D2, INPUT);
  pinMode(D3, OUTPUT);
  // pinMode(D3, INPUT);
  pinMode(D4, OUTPUT);
  // pinMode(D4, INPUT);
  pinMode(D5, OUTPUT);
  // pinMode(D5, INPUT);
  pinMode(D6, OUTPUT);
  // pinMode(D6, INPUT);
  pinMode(D7, OUTPUT);
  // pinMode(D7, INPUT);
  pinMode(D8, OUTPUT);
  // pinMode(D8, INPUT);
  pinMode(D9, OUTPUT);
  // pinMode(D9, INPUT);
  pinMode(D10, OUTPUT);
  // pinMode(D10, INPUT);
  pinMode(D11, OUTPUT);
  // pinMode(D11, INPUT);
  pinMode(D12, OUTPUT);
  // pinMode(D12, INPUT);
  pinMode(D13, OUTPUT);
  // pinMode(D13, INPUT);
}
