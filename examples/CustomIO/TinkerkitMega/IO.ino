void customIO() {
  // Input pin directions
  pinMode(I0, INPUT);
  pinMode(I1, INPUT);
  pinMode(I2, INPUT);
  pinMode(I3, INPUT);
  pinMode(I4, INPUT);
  pinMode(I5, INPUT);
  pinMode(I6, INPUT);
  pinMode(I7, INPUT);
  pinMode(I8, INPUT);
  pinMode(I9, INPUT);
  
  // Output pin directions
  pinMode(O0, OUTPUT);
  pinMode(O1, OUTPUT);
  pinMode(O2, OUTPUT);
  pinMode(O3, OUTPUT);
  pinMode(O4, OUTPUT);
  pinMode(O5, OUTPUT);
  
  // Digital Pin definitions (delete/enable as required)
  // pinMode(D13, INPUT);
  // pinMode(D13, OUTPUT);
  // pinMode(D12, INPUT);
  // pinMode(D12, OUTPUT);
  // pinMode(D8, INPUT);
  // pinMode(D8, OUTPUT);
  // pinMode(D7, INPUT);
  // pinMode(D7, OUTPUT);
  // pinMode(D4, INPUT);
  // pinMode(D4, OUTPUT);
  // pinMode(D2, INPUT);
  // pinMode(D2, OUTPUT);
}

