void customIO() {
  // Input pin directions
  pinMode(A0, INPUT);
  pinMode(A1, INPUT);
  pinMode(A2, INPUT);
  pinMode(A3, INPUT);
  pinMode(A4, INPUT);
  
  // Input/Output pin directions (delete / enable as appropriate)
  pinMode(D2, OUTPUT);
  // pinMode(D2, INPUT);
  pinMode(D3, OUTPUT);
  // pinMode(D3, INPUT);
  pinMode(D4, OUTPUT);
  // pinMode(D4, INPUT);
  pinMode(D5, OUTPUT);
  // pinMode(D5, INPUT);
  pinMode(D6, OUTPUT);
  // pinMode(D6, INPUT);
  pinMode(D7, OUTPUT);
  // pinMode(D7, INPUT);
  pinMode(D8, OUTPUT);
  // pinMode(D8, INPUT);
  pinMode(D9, OUTPUT);
  // pinMode(D9, INPUT);

}

