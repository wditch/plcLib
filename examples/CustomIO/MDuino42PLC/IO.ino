void customIO() {
  // Input pin directions
  // B Zone inputs
  pinMode(I00, INPUT);
  pinMode(I01, INPUT);
  pinMode(I02, INPUT);
  pinMode(I03, INPUT);
  pinMode(I04, INPUT);
  pinMode(I05, INPUT);
  pinMode(I06, INPUT);
  pinMode(I07, INPUT);
  pinMode(I08, INPUT);
  pinMode(I09, INPUT);
  pinMode(I010, INPUT);
  pinMode(I011, INPUT);
  pinMode(I012, INPUT);
  
  // C Zone inputs
  pinMode(I10, INPUT);
  pinMode(I11, INPUT);
  pinMode(I12, INPUT);
  pinMode(I13, INPUT);
  pinMode(I14, INPUT);
  pinMode(I15, INPUT);
  pinMode(I16, INPUT);
  pinMode(I17, INPUT);
  pinMode(I18, INPUT);
  pinMode(I19, INPUT);
  pinMode(I110, INPUT);
  pinMode(I111, INPUT);
  pinMode(I112, INPUT);

  // Output pin directions
  // B Zone outputs
  pinMode(Q00, OUTPUT);
  pinMode(Q01, OUTPUT);
  pinMode(Q02, OUTPUT);
  pinMode(Q03, OUTPUT);
  pinMode(Q04, OUTPUT);
  pinMode(Q05, OUTPUT);
  pinMode(Q06, OUTPUT);
  pinMode(Q07, OUTPUT);

  // C Zone outputs
  pinMode(Q10, OUTPUT);
  pinMode(Q11, OUTPUT);
  pinMode(Q12, OUTPUT);
  pinMode(Q13, OUTPUT);
  pinMode(Q14, OUTPUT);
  pinMode(Q15, OUTPUT);
  pinMode(Q16, OUTPUT);
  pinMode(Q17, OUTPUT);
}

