void customIO() {
  // Input pin directions
  pinMode(I00, INPUT);
  pinMode(I01, INPUT);
  pinMode(I02, INPUT);
  pinMode(I03, INPUT);
  pinMode(I04, INPUT);
  pinMode(I05, INPUT);
  pinMode(I06, INPUT);
  pinMode(I07, INPUT);
  pinMode(I08, INPUT);
  pinMode(I09, INPUT);
  
  // Output pin directions
  pinMode(R1, OUTPUT);
  pinMode(R2, OUTPUT);
  pinMode(R3, OUTPUT);
  pinMode(R4, OUTPUT);
  pinMode(R5, OUTPUT);
  pinMode(R6, OUTPUT);
  pinMode(R7, OUTPUT);
  pinMode(R8, OUTPUT);
  pinMode(Q00, OUTPUT);
  pinMode(Q01, OUTPUT);
}

